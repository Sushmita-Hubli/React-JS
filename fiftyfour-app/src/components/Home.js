import React from 'react'

const Home = () => {
  return (
    <div>
      Link → Navigates to a different route in your React app (basic navigation).
<br></br>
NavLink → Works like Link but adds styling (like an active class) automatically when the link matches the current URL — useful for highlighting the active page in a navbar.
    </div>
  )
}

export default Home
